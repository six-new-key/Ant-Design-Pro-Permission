# 通用后台权限管理系统 前端开发指南

> 本文档旨在帮助团队成员快速理解项目架构、掌握开发规范、高效协作开发。

---

## 目录

1. [项目概述](#一项目概述)
2. [技术架构](#二技术架构)
3. [核心模块解析](#三核心模块解析)
4. [开发环境配置](#四开发环境配置)
5. [开发工作流](#五开发工作流)
6. [代码规范详解](#六代码规范详解)
7. [常见问题与解决方案](#七常见问题与解决方案)
8. [附录](#八附录)

---

## 一、项目概述

### 1.1 项目简介

通用后台权限管理系统 前端项目是一个基于 Vue 3 生态的企业级中后台管理系统模板，采用模块化、组件化的设计理念，支持多种布局模式、动态主题切换、国际化等特性。

### 1.2 核心特性

- **多种布局模式**：侧边栏布局、顶部导航布局、混合布局、双列布局、全屏布局
- **动态主题**：支持亮/暗模式切换，支持自定义主题色
- **权限系统**：基于 RBAC 的细粒度权限控制
- **动态路由**：后端返回路由配置，前端动态渲染
- **国际化支持**：基于 vite-auto-i18n-plugin 的自动翻译方案
- **响应式设计**：适配不同屏幕尺寸

### 1.3 项目结构概览

```
yjsa-front/
├── src/                      # 源代码目录
│   ├── api/                  # API 接口层
│   ├── assets/               # 静态资源
│   ├── components/           # 公共组件库
│   ├── constants/            # 常量定义
│   ├── directives/           # 自定义指令
│   ├── layouts/              # 布局组件
│   ├── locale/               # 国际化
│   ├── plugins/              # 插件注册
│   ├── router/               # 路由配置
│   ├── stores/               # Pinia 状态管理
│   ├── style/                # 全局样式
│   ├── utils/                # 工具函数
│   └── views/                # 页面视图
├── index.html                # HTML 模板
├── package.json              # 依赖配置
└── vite.config.js            # Vite 配置
```

---

## 二、技术架构

### 2.1 技术栈选型

#### 2.1.1 核心框架

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **Vue 3** | ^3.5.22 | 组合式 API 更好的逻辑复用，性能优化，TypeScript 支持更好 |
| **Vite** | ^7.1.2 | 快速的冷启动，即时的模块热更新，优化的构建 |
| **Vue Router 4** | ^4.5.1 | 官方路由管理，支持动态路由添加 |
| **Pinia** | ^3.0.3 | 官方推荐状态管理，TypeScript 友好，更好的开发体验 |

#### 2.1.2 UI 组件库

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **Ant Design Vue** | ^4.2.6 | 企业级 UI 设计，组件丰富，文档完善 |
| **Ant Design X Vue** | ^1.3.2 | AI 场景组件，支持 Chat 对话界面 |
| **@ant-design/icons-vue** | ^7.0.1 | 官方图标库，与 Ant Design 风格一致 |

#### 2.1.3 功能库

| 技术 | 用途 | 说明 |
|------|------|------|
| **Axios** | HTTP 请求 | 支持拦截器、请求/响应转换 |
| **js-cookie** | Cookie 操作 | 简化 Cookie 读写 |
| **crypto-js** | 加密解密 | 敏感数据处理 |
| **lodash** | 工具函数 | 节流、防抖等常用函数 |
| **nprogress** | 进度条 | 路由切换进度指示 |
| **pinia-plugin-persistedstate** | 状态持久化 | Store 数据本地存储 |

#### 2.1.4 开发工具

| 技术 | 用途 | 说明 |
|------|------|------|
| **unplugin-vue-components** | 自动组件导入 | 无需手动 import 组件 |
| **vite-plugin-svg-icons** | SVG 图标管理 | SVG 雪碧图自动生成 |
| **vite-auto-i18n-plugin** | 自动国际化 | 自动提取中文并翻译 |
| **sass-embedded** | CSS 预处理器 | 更快的 SCSS 编译 |

### 2.2 架构设计

#### 2.2.1 分层架构

```
┌─────────────────────────────────────────────┐
│                    视图层 (Views)              │
│    - 页面组件、业务逻辑、UI 组装               │
├─────────────────────────────────────────────┤
│                   组件层 (Components)          │
│    - 布局组件、业务组件、基础组件              │
├─────────────────────────────────────────────┤
│                   状态层 (Stores)              │
│    - 全局状态、用户信息、主题配置              │
├─────────────────────────────────────────────┤
│                   服务层 (API)                 │
│    - 接口定义、请求封装、数据转换              │
├─────────────────────────────────────────────┤
│                   工具层 (Utils)               │
│    - 通用函数、常量、配置                      │
└─────────────────────────────────────────────┘
```

#### 2.2.2 数据流设计

```
后端 API
   ↓
API 层 (src/api/)
   ↓
请求拦截器 (request.js) → 统一错误处理
   ↓
Store 层 (src/stores/) → 状态管理
   ↓
组件/视图层 → UI 渲染
   ↓
用户交互 → 调用 Store Action → API 请求
```

#### 2.2.3 路由设计

```
常量路由 (constantRoutes)
  ├─ /login - 登录页
  ├─ /404 - 404 页
  └─ 其他公共页面

动态路由 (dynamicRoutes)
  └─ 从后端获取路由配置
     └─ 动态加载组件 (import.meta.glob)
        └─ 包装在 LayoutManager 中
           └─ 渲染到 router-view
```

---

## 三、核心模块解析

### 3.1 路由与权限系统

#### 3.1.1 路由配置 (router/index.js)

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import { constantRoutes } from '@/router/routes'

const router = createRouter({
  history: createWebHistory(),
  routes: constantRoutes,  // 初始只加载常量路由
  scrollBehavior() {
    return { top: 0 }  // 切换路由时滚动到顶部
  }
})

export default router
```

#### 3.1.2 权限控制 (permission.js)

```javascript
// 全局前置守卫
router.beforeEach(async (to, from, next) => {
  NProgress.start()
  
  const accessToken = AuthUtils.getAccessToken()
  
  // 1. 已登录状态访问登录页，重定向到首页
  if (accessToken && to.path === LOGIN_PATH) {
    next({ path: HOME_PATH })
    return
  }
  
  // 2. 需要登录的页面
  if (!to.meta?.noAuth && !accessToken) {
    next({ path: LOGIN_PATH, query: { redirect: to.fullPath } })
    return
  }
  
  // 3. 已登录且未加载路由，获取用户信息和路由
  if (accessToken && !userStore.hasAddedRoutes) {
    await userStore.getUserInfo()
    next({ ...to, replace: true })
    return
  }
  
  next()
})
```

#### 3.1.3 动态路由处理 (stores/modules/user.js)

```javascript
// 动态导入所有组件
const modules = import.meta.glob('/src/**/*.vue')

const processRoutes = (routes) => {
  return routes.map(route => {
    const processedRoute = {
      path: route.path,
      name: route.name,
      meta: route.meta || {}
    }
    
    // 处理组件加载
    if (route.meta?.isIframe) {
      // iframe 类型
      processedRoute.component = modules['/src/components/core/IframeView.vue']
    } else if (route.component) {
      // 普通组件动态加载
      processedRoute.component = modules[route.component]
    }
    
    // 递归处理子路由
    if (route.children?.length > 0) {
      processedRoute.children = processRoutes(route.children)
    }
    
    return processedRoute
  })
}
```

### 3.2 状态管理 (Pinia)

#### 3.2.1 Store 组织结构

```javascript
// stores/index.js
import { createPinia } from 'pinia'
import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'

const pinia = createPinia()
pinia.use(piniaPluginPersistedstate)

export default pinia
```

#### 3.2.2 各 Store 职责

| Store | 职责 | 持久化 |
|-------|------|--------|
| **useUserStore** | 用户信息、权限、角色、路由 | userData, permissions, roles |
| **useAppStore** | 应用配置、布局、语言、全屏 | language, layout, themeMode |
| **useThemeStore** | 主题色、圆角、字体大小 | primaryColorHex, baseConfig |
| **useTabsStore** | 页签管理、激活状态、固定状态 | activeTabs, activeTabPath |
| **useLoginStore** | 登录页配置、背景设置 | selectedStaticBg, formPosition |

#### 3.2.3 Store 使用示例

```javascript
// 1. 获取 Store 实例
import { useUserStore, useAppStore } from '@/stores'

const userStore = useUserStore()
const appStore = useAppStore()

// 2. 访问 State（响应式）
console.log(userStore.userData)

// 3. 访问 Getters
if (userStore.isAdmin) {
  // 管理员逻辑
}

// 4. 调用 Actions
await userStore.handleLogin(formData)
await appStore.setLayout('sidebar')
```

### 3.3 HTTP 请求封装

#### 3.3.1 Axios 配置 (utils/request.js)

```javascript
import axios from 'axios'
import JSONBig from 'json-bigint'

const jsonBig = JSONBig({ storeAsString: true })

const request = axios.create({
  baseURL: import.meta.env.VITE_APP_BASE_API,
  timeout: 60000,
  headers: { 'Content-Type': 'application/json' },
  // 处理大数字精度问题
  transformResponse: [(data, headers) => {
    if (headers?.['content-type']?.includes('application/octet-stream')) {
      return data
    }
    if (typeof data === 'string') {
      try {
        return jsonBig.parse(data)
      } catch (err) {
        return data
      }
    }
    return data
  }]
})
```

#### 3.3.2 请求拦截器

```javascript
request.interceptors.request.use((config) => {
  const accessToken = AuthUtils.getAccessToken()
  if (accessToken) {
    config.headers['Authorization'] = accessToken
  }
  return config
})
```

#### 3.3.3 响应拦截器与 Token 刷新

```javascript
let isRefreshing = false
let requestQueue = []

request.interceptors.response.use(
  async (response) => {
    const res = response.data
    const code = res.code
    const originalRequest = response.config
    
    // 401 Token 过期，自动刷新
    if (code === 401) {
      // 避免重复刷新
      if (originalRequest._retry) {
        userStore.handleLogout()
        return Promise.reject(new Error(res.message))
      }
      
      originalRequest._retry = true
      
      if (isRefreshing) {
        // 等待刷新完成
        return new Promise((resolve) => {
          requestQueue.push((newToken) => {
            originalRequest.headers['Authorization'] = newToken
            resolve(request(originalRequest))
          })
        })
      }
      
      isRefreshing = true
      try {
        const refreshRes = await refreshTokenApi()
        if (refreshRes.code === 200) {
          const { accessToken } = refreshRes.data
          AuthUtils.setTokens(refreshRes.data)
          
          // 重试队列中的请求
          requestQueue.forEach(cb => cb(accessToken))
          requestQueue = []
          
          isRefreshing = false
          return request(originalRequest)
        }
      } catch (error) {
        isRefreshing = false
        userStore.handleLogout()
        return Promise.reject(error)
      }
    }
    
    // 其他错误处理...
    if (code !== 200) {
      message.warning(res.message)
      return Promise.reject(res)
    }
    
    return res
  }
)
```

### 3.4 布局系统

#### 3.4.1 布局管理器 (layouts/LayoutManager.vue)

```
LayoutManager
  ├─ 根据 appStore.layout 动态渲染不同布局
  │   ├─ 'sidebar' → SidebarLayout
  │   ├─ 'topbar' → TopbarLayout
  │   ├─ 'mixed' → MixedLayout
  │   ├─ 'doublecolumn' → DoubleColumnLayout
  │   └─ 'fullscreen' → FullscreenLayout
  │
  └─ 公共组件
      ├─ TabsView (页签栏)
      ├─ AppSettingsDrawer (设置抽屉)
      └─ LockScreen (锁屏)
```

#### 3.4.2 布局切换原理

```javascript
// stores/modules/app.js
const layout = ref('sidebar')  // 默认侧边栏布局

const setLayout = (newLayout) => {
  layout.value = newLayout
}

// LayoutManager.vue
<template>
  <SidebarLayout v-if="appStore.layout === 'sidebar'" />
  <TopbarLayout v-else-if="appStore.layout === 'topbar'" />
  <MixedLayout v-else-if="appStore.layout === 'mixed'" />
  <!-- ... -->
</template>
```

### 3.5 主题系统

#### 3.5.1 主题配置 (App.vue)

```javascript
import { theme } from 'ant-design-vue'

const themeConfig = computed(() => {
  return {
    // 暗色/亮色算法
    algorithm: appStore.themeMode === 'dark' 
      ? theme.darkAlgorithm 
      : theme.defaultAlgorithm,
    token: {
      colorPrimary: themeStore.primaryColorHex,      // 主题色
      colorInfo: themeStore.primaryColorHex,
      colorBgLayout: appStore.themeMode === 'dark' ? settings.bgColor : '#eee',
      fontSize: themeStore.baseConfig.fontSize,       // 字体大小
      borderRadius: themeStore.baseConfig.borderRadius, // 圆角
      wireframe: themeStore.baseConfig.wireframe      // 线框风格
    }
  }
})
```

#### 3.5.2 主题变量同步

```javascript
// settings.js - JS 配置
export const settings = {
  sidebarWidth: 228,
  sidebarWidthCollapsed: 48,
  // ...
}

// global.scss - SCSS 变量
$sidebar-width: 228px;
$sidebar-width-collapsed: 48px;

// 使用时需保持同步
```

### 3.6 页签系统

#### 3.6.1 页签状态管理

```javascript
// stores/modules/tabsStore.js
const activeTabs = ref([])        // 打开的页签列表
const activeTabPath = ref('')     // 当前激活页签路径

// 添加页签
const addTab = (route) => {
  const exists = activeTabs.value.some(tab => tab.path === route.path)
  if (!exists) {
    activeTabs.value.push({
      path: route.path,
      title: route.meta.title,
      icon: route.meta.icon,
      closable: route.meta.closable !== false,
      pinned: false
    })
  }
  activeTabPath.value = route.path
}

// 固定/取消固定
const toggleTabPin = (path) => {
  const tab = activeTabs.value.find(t => t.path === path)
  if (tab && tab.path !== HOME_PATH) {
    tab.pinned = !tab.pinned
  }
}
```

#### 3.6.2 页签与路由同步

```javascript
// TabsView.vue
watch(() => route.path, (newPath) => {
  if (newPath) {
    tabsStore.addTab(route)
    nextTick(() => {
      scrollToActiveTab()
    })
  }
}, { immediate: true })
```

### 3.7 字典组件

#### 3.7.1 字典数据管理

```javascript
// utils/useDict.js
import { ref, onMounted } from 'vue'
import { queryDictDataByType } from '@/api/dict'

export function useDict(dictType) {
  const dict = ref([])
  const loading = ref(false)
  
  const loadDict = async () => {
    loading.value = true
    try {
      const res = await queryDictDataByType(dictType)
      if (res.code === 200) {
        dict.value = res.data
      }
    } finally {
      loading.value = false
    }
  }
  
  onMounted(loadDict)
  
  return { dict, loading, refresh: loadDict }
}
```

#### 3.7.2 字典组件使用

```vue
<template>
  <!-- 字典下拉框 -->
  <DictSelect 
    v-model:value="form.status" 
    dict-type="user_status"
    placeholder="请选择状态"
  />
  
  <!-- 字典标签 -->
  <DictTag :value="record.status" dict-type="user_status" />
  
  <!-- 字典单选框 -->
  <DictRadio v-model:value="form.gender" dict-type="gender" />
</template>
```

---

## 四、开发环境配置

### 4.1 环境要求

| 工具 | 最低版本 | 推荐版本 |
|------|----------|----------|
| Node.js | 18.x | 20.x LTS |
| npm | 9.x | 10.x |
| VSCode | 1.80 | 最新版 |

### 4.2 快速开始

```bash
# 1. 克隆项目
git clone <repository-url>
cd yjsa-front

# 2. 安装依赖
npm install

# 3. 配置环境变量
# 复制 .env.example 为 .env.development
# 根据实际情况修改 API 地址

# 4. 启动开发服务器
npm run dev

# 5. 打开浏览器访问 http://localhost:5173
```

### 4.3 环境变量配置

```
# .env.development
VITE_APP_TITLE=YJSA管理系统
VITE_APP_BASE_API=/api
VITE_DEV_SERVE=http://localhost:8080
```

```
# .env.production
VITE_APP_TITLE=YJSA管理系统
VITE_APP_BASE_API=/api
VITE_PROD_SERVE=https://api.yjsa.com
```

### 4.4 VSCode 配置

#### 4.4.1 推荐插件

```json
// .vscode/extensions.json
{
  "recommendations": [
    "Vue.volar",              // Vue 3 官方插件
    "dbaeumer.vscode-eslint", // ESLint
    "esbenp.prettier-vscode", // Prettier
    "antfu.auto-import",      // 自动导入
    "formulahendry.auto-rename-tag" // 自动重命名标签
  ]
}
```

#### 4.4.2 项目设置

```json
// .vscode/settings.json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "editor.quickSuggestions": {
    "strings": true
  }
}
```

### 4.5 代理配置

```javascript
// vite.config.js
server: {
  port: 5173,
  proxy: {
    [env.VITE_APP_BASE_API]: {
      target: env.VITE_DEV_SERVE,
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, '')
    }
  }
}
```

---

## 五、开发工作流

### 5.1 功能开发流程

```
1. 需求分析
   └─ 理解需求，确定涉及模块

2. 接口对接
   └─ 与后端确认接口定义
   └─ 在 src/api/ 下创建 API 模块

3. 页面开发
   └─ 创建视图组件 (src/views/)
   └─ 如需复用逻辑，创建 composable
   └─ 编写组件代码

4. 联调测试
   └─ 对接后端接口
   └─ 处理边界情况

5. 代码审查
   └─ 自测通过后可提交代码
```

### 5.2 新增页面步骤

#### 步骤 1：创建 API 模块

```javascript
// src/api/product/index.js
import { request } from '@/utils'

const PREFIX = '/product'

export function queryProductList(params) {
  return request({
    url: `${PREFIX}/list`,
    method: 'POST',
    data: params
  })
}

export function addProduct(data) {
  return request({
    url: `${PREFIX}/add`,
    method: 'POST',
    data
  })
}
```

#### 步骤 2：创建 Composables（可选）

```javascript
// src/views/product/composables/useProductTable.js
import { useTable } from '@/composables/useTable'
import { queryProductList, deleteProduct } from '@/api/product'

export function useProductTable() {
  return useTable(queryProductList, {
    immediate: true,
    deleteApi: deleteProduct
  })
}
```

#### 步骤 3：创建页面组件

```vue
<template>
  <div class="product-container">
    <!-- 搜索区域 -->
    <a-card :bordered="false" v-show="searchVisible">
      <a-form layout="inline" :model="searchForm">
        <a-form-item>
          <a-input v-model:value="searchForm.name" placeholder="请输入产品名称" />
        </a-form-item>
        <a-form-item>
          <a-button type="primary" @click="handleSearch">搜索</a-button>
        </a-form-item>
      </a-form>
    </a-card>
    
    <!-- 表格区域 -->
    <a-card :bordered="false">
      <a-table 
        :columns="columns" 
        :data-source="dataSource"
        :pagination="pagination"
        :loading="loading"
        @change="handleTableChange"
      />
    </a-card>
  </div>
</template>

<script setup>
import { useProductTable } from './composables/useProductTable'

const {
  loading,
  dataSource,
  pagination,
  searchForm,
  handleSearch,
  handleTableChange
} = useProductTable()

const columns = [
  { title: '产品名称', dataIndex: 'name' },
  { title: '价格', dataIndex: 'price' },
  // ...
]
</script>
```

#### 步骤 4：配置菜单权限

后端管理系统配置菜单：
- 路径: `/product/list`
- 组件: `/src/views/product/ProductList.vue`
- 权限标识: `product:list`

### 5.3 新增组件步骤

#### 步骤 1：确定组件类型

| 类型 | 位置 | 说明 |
|------|------|------|
| 核心组件 | `src/components/core/` | 布局相关，如 TabsView |
| 业务组件 | `src/components/custom/` | 通用业务组件，如 DictSelect |
| 页面组件 | `src/views/xxx/components/` | 页面专属组件 |

#### 步骤 2：编写组件

```vue
<!-- src/components/custom/StatusTag.vue -->
<template>
  <a-tag :color="color">{{ label }}</a-tag>
</template>

<script setup>
const props = defineProps({
  status: {
    type: String,
    required: true
  }
})

const statusMap = {
  active: { label: '启用', color: 'success' },
  inactive: { label: '禁用', color: 'error' }
}

const { label, color } = statusMap[props.status] || {}
</script>
```

#### 步骤 3：注册组件（全局）

```javascript
// src/plugins/index.js
import StatusTag from '@/components/custom/StatusTag.vue'

export default {
  install(app) {
    app.component('StatusTag', StatusTag)
  }
}
```

或使用自动导入：
```javascript
// vite.config.js
Components({
  resolvers: [
    AntDesignVueResolver(),
    // 自动导入 src/components 下的组件
  ],
  dirs: ['src/components']
})
```

### 5.4 Git 工作流

```bash
# 1. 从主分支拉取最新代码
git checkout main
git pull origin main

# 2. 创建功能分支
git checkout -b feat/user-management

# 3. 开发并提交代码
git add .
git commit -m "feat(user): 增加用户管理页面"

# 4. 推送到远程
git push origin feat/user-management

# 5. 创建 Pull Request 进行代码审查

# 6. 审查通过后合并到主分支
```

---

## 六、代码规范详解

### 6.1 Vue 组件规范

#### 6.1.1 组件结构

```vue
<template>
  <!-- 模板内容 -->
</template>

<script setup>
/**
 * @description 组件功能说明
 */

// 1. 导入依赖
import { ref, computed } from 'vue'

// 2. Props & Emits
const props = defineProps({})
const emit = defineEmits([])

// 3. 注入依赖
const store = useUserStore()

// 4. 状态定义
const loading = ref(false)

// 5. 计算属性
const canSubmit = computed(() => {})

// 6. 方法定义
const handleSubmit = async () => {}

// 7. 生命周期
onMounted(() => {})
</script>

<style scoped lang="scss">
/* 样式 */
</style>
```

#### 6.1.2 Props 定义规范

```javascript
const props = defineProps({
  // 必填项放前面
  id: {
    type: String,
    required: true
  },
  
  // 带默认值的放后面
  type: {
    type: String,
    default: 'default'
  },
  
  // 复杂类型使用函数返回
  config: {
    type: Object,
    default: () => ({})
  },
  
  // 数组类型
  list: {
    type: Array,
    default: () => []
  },
  
  // 自定义验证
  status: {
    type: String,
    validator: (value) => ['active', 'inactive'].includes(value)
  }
})
```

### 6.2 API 层规范

#### 6.2.1 模块划分

按业务模块划分 API 文件夹：
```
api/
├── auth/          # 认证相关
├── user/          # 用户管理
├── role/          # 角色管理
├── menu/          # 菜单管理
├── dict/          # 字典管理
└── ...
```

#### 6.2.2 接口命名规范

| 操作 | 命名 | 示例 |
|------|------|------|
| 查询列表 | queryXxxList | queryUserList |
| 查询单个 | queryXxxById | queryUserById |
| 查询自身 | querySelf | querySelf |
| 新增 | addXxx | addUser |
| 修改 | updateXxx | updateUser |
| 删除 | deleteXxx | deleteUser |
| 批量删除 | batchDeleteXxx | batchDeleteUser |
| 导出 | exportXxx | exportUser |
| 导入 | importXxx | importUser |
| 状态切换 | toggleXxxStatus | toggleUserStatus |

### 6.3 Store 规范

#### 6.3.1 Store 结构

```javascript
export const useXxxStore = defineStore(
  'storeName',
  () => {
    // ============ State ============
    const state = ref(initialValue)
    
    // ============ Getters ============
    const computedValue = computed(() => {})
    
    // ============ Actions ============
    const action = async () => {}
    
    return {
      state,
      computedValue,
      action
    }
  },
  {
    persist: {
      key: 'store-key',
      storage: localStorage,
      pick: ['state']
    }
  }
)
```

#### 6.3.2 Store 使用原则

1. **组件内优先使用解构**：保持响应性
```javascript
// 正确
const userStore = useUserStore()
const { userData } = storeToRefs(userStore)

// 错误 - 失去响应性
const { userData } = useUserStore()
```

2. **Action 中处理错误**：
```javascript
const fetchData = async () => {
  try {
    const res = await api.queryList()
    list.value = res.data
  } catch (error) {
    message.error(error.message)
    throw error  // 继续抛出供调用方处理
  }
}
```

### 6.4 样式规范

#### 6.4.1 SCSS 变量使用

```scss
// 使用全局变量
.container {
  padding: $content-padding;
  background: $bg-color;
}

// 组件局部变量
<style scoped lang="scss">
$card-padding: 16px;

.card {
  padding: $card-padding;
}
</style>
```

#### 6.4.2 主题色使用

```vue
<template>
  <div :style="{ color: token.colorPrimary }">
  </div>
</template>

<script setup>
import { theme } from 'ant-design-vue'
const { token } = theme.useToken()
</script>
```

### 6.5 注释规范

#### 6.5.1 文件头注释

```javascript
/**
 * @description 用户管理 API 模块
 * @author 作者名
 * @date 2024-01-01
 * @version 1.0.0
 */
```

#### 6.5.2 函数注释

```javascript
/**
 * 分页查询用户列表
 * @param {Object} params - 查询参数
 * @param {number} params.pageNum - 页码
 * @param {number} params.pageSize - 每页条数
 * @returns {Promise<Object>} 返回分页数据
 * @throws {Error} 请求失败时抛出错误
 */
const queryUserList = async (params) => {
  // ...
}
```

#### 6.5.3 代码分段注释

```javascript
// ==================== 状态定义 ====================
const loading = ref(false)

// ==================== 计算属性 ====================
const canSubmit = computed(() => {})

// ==================== 方法定义 ====================
const handleSubmit = () => {}

// ==================== 生命周期 ====================
onMounted(() => {})
```

---

## 七、常见问题与解决方案

### 7.1 路由相关问题

#### Q1: 新页面无法访问，显示 404

**原因**：动态路由未正确加载

**解决**：
1. 检查后端返回的路由配置中 `component` 路径是否正确
2. 检查组件文件是否存在于 `src/views/` 下
3. 检查路由配置中的 `name` 是否唯一

```javascript
// 正确路径格式
{
  component: '/src/views/product/ProductList.vue'
}
```

#### Q2: 页面刷新后显示空白

**原因**：动态路由在刷新时丢失

**解决**：确保在路由守卫中正确获取用户信息：
```javascript
if (accessToken && !userStore.hasAddedRoutes) {
  await userStore.getUserInfo()
  next({ ...to, replace: true })
  return
}
```

### 7.2 状态管理相关问题

#### Q1: Store 数据刷新后丢失

**原因**：未配置持久化或配置错误

**解决**：
```javascript
{
  persist: {
    key: 'user-store',
    storage: localStorage,
    pick: ['userData', 'permissions']  // 确保需要持久化的字段在 pick 中
  }
}
```

#### Q2: 解构 Store 后失去响应性

**原因**：直接解构会丢失响应性

**解决**：
```javascript
import { storeToRefs } from 'pinia'

const userStore = useUserStore()
const { userData, permissions } = storeToRefs(userStore)
// 方法可以直接解构
const { fetchUserInfo } = userStore
```

### 7.3 API 请求相关问题

#### Q1: 请求返回 401 但未自动跳转登录

**原因**：可能是刷新 Token 逻辑问题

**解决**：检查 `request.js` 中的拦截器逻辑，确保在刷新失败时调用 `handleLogout`

#### Q2: 大数字精度丢失

**原因**：JavaScript 数字精度限制

**解决**：项目已配置 `json-bigint`，确保后端返回的是字符串类型的大数字

### 7.4 组件相关问题

#### Q1: Ant Design 组件样式不生效

**原因**：样式未正确导入

**解决**：确保在 `main.js` 中导入样式：
```javascript
import 'ant-design-vue/dist/reset.css'
```

#### Q2: 字典组件不显示数据

**原因**：字典类型错误或数据未加载

**解决**：
1. 检查 `dictType` 是否正确
2. 检查网络请求是否正常
3. 使用浏览器开发者工具检查组件状态

### 7.5 性能优化建议

#### 大列表优化

```javascript
// 使用虚拟滚动
import { useVirtualList } from '@vueuse/core'

const { list, containerProps, wrapperProps } = useVirtualList(
  largeList,
  { itemHeight: 50 }
)
```

#### 组件懒加载

```javascript
// 路由组件懒加载
const UserManagement = () => import('@/views/system/UserManagement.vue')

// 异步组件
const AsyncComponent = defineAsyncComponent(() => 
  import('@/components/HeavyComponent.vue')
)
```

---

## 八、附录

### 8.1 常用命令速查

```bash
# 开发
npm run dev          # 启动开发服务器

# 构建
npm run build        # 生产构建
npm run preview      # 预览生产构建

# 依赖
npm install          # 安装依赖
npm update           # 更新依赖
```

### 8.2 项目配置速查

| 配置项 | 文件 | 说明 |
|--------|------|------|
| 环境变量 | `.env.development` | 开发环境配置 |
| Vite 配置 | `vite.config.js` | 构建工具配置 |
| 全局设置 | `src/settings.js` | 项目全局常量 |
| 样式变量 | `src/style/global.scss` | SCSS 变量定义 |

### 8.3 浏览器兼容性

- Chrome >= 80
- Firefox >= 75
- Safari >= 13
- Edge >= 80

### 8.4 相关文档链接

- [Vue 3 官方文档](https://vuejs.org/)
- [Ant Design Vue 文档](https://www.antdv.com/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [Vite 文档](https://vitejs.dev/)

